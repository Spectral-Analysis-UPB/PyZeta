.. _package_reference:

Package Reference
=================

.. toctree::
   :maxdepth: 1

   core
   geometry
   framework
   persistence
   view
   experiments
   utils

.. warning::

   This package index contains only most but *not all* classes in the respective
   packages. Excluded are e.g. custom exceptions which do not add essential new
   functionality. Note also that this index might not always be completely up to
   date (it might be missing some of the latest classes or existing doc strings
   might not have been updated yet).

